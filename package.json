{
  "name": "tictactoe-3d",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && git add -A && git commit -m 'Deploy with cache bust' && git push origin gh-pages"
  }
}
